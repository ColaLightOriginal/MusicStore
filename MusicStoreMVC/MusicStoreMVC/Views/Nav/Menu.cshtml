@model IEnumerable<string>

@Html.ActionLink("Wszystkie albumy", "Albumy", "Album")

@foreach (var item in Model)
{
		@Html.RouteLink(item, new
	{
		controller = "Album",
		action = "Albumy",
		genre = item,
		page = 1
	})
}
